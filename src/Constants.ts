const prod = {
  url: {
    BASE_URL: ""
  }
};

const dev = {
  url: {
    BASE_URL: "http://task.server.brainstormtech.io"
  }
};

//TODO in the future we can check environment is prod or dev
export const config = dev;
